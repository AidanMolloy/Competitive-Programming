#include <bits/stdc++.h>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int x;
	cin >> x;

	for(int i=0; i<x;i++){
		int n, m;
		cin >> n >> m;
		cout << n-1 << "\n";

		for(int i=0;i<m;i++){
			int a, b;
			cin >> a >> b;
		}
	}

	return 0;
}
