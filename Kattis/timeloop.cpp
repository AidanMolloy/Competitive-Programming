#include <bits/stdc++.h>

using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	int n;
	cin >> n;

	int counter = 1;
	while (counter <= n){
		cout << counter << " Abracadabra\n";
		counter++;
	}
	return 0;
}
